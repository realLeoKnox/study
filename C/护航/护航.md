# 运算符优先级
赋值运算符
关系运算符 < !=
算数运算符 +
逻辑运算符 &&

# 条件表达式等价的逻辑表达式
x%3 作条件表达式时等价于表达式（x%3!=0）

# 数学关系表达式使用的表达式
x>=y>=z ==> (x>=y)&&(y>=z)

# 判断char型变量是否为大小写字母的表达式
char型变量ch ==> (ch>='A')&&(ch<='Z')

# 逻辑表达式的判断

# if语句

# 程序运行结果

# switch case选择语句以及break语句使用时机